<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Anime Hybrid Recommendation System</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>

<div class="container mt-5">
    <h1 class="text-center mb-5">Anime Recommendation System</h1>

    <!-- User Input Form -->
    <div class="custom-card p-4 shadow-sm">
        <form action="/" method="POST" id="recommendForm">
            <div class="mb-3">
                <label for="UserID" class="form-label">Enter User ID</label>
                <input type="number" class="custom-input form-control" id="UserID" name="UserID"
                       placeholder="e.g. 123" required value="{{ user_id }}">
            </div>
            <button type="submit" class="custom-btn w-100">Get Recommendations</button>
        </form>
    </div>

    <!-- Alert -->
    {% if submitted and recommendations|length == 0 %}
        <div class="custom-alert mt-4">
            No recommendations found for this user. Please try another User ID.
        </div>
    {% endif %}

    <!-- Recommendations Accordion -->
    {% if recommendations %}
    <div class="accordion mt-4" id="animeAccordion">
        {% for rec in recommendations %}
        <div class="accordion-item custom-card">
            <h2 class="accordion-header" id="heading{{ loop.index }}">
                <button class="accordion-button collapsed btn-link" type="button"
                        data-bs-toggle="collapse" data-bs-target="#collapse{{ loop.index }}"
                        aria-expanded="false" aria-controls="collapse{{ loop.index }}">
                    {{ rec.anime }}
                </button>
            </h2>
            <div id="collapse{{ loop.index }}" class="accordion-collapse collapse"
                 aria-labelledby="heading{{ loop.index }}" data-bs-parent="#animeAccordion">
                <div class="accordion-body card-text">
                    {{ rec.synopsis }}
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
    {% endif %}
</div>

<!-- Loading Spinner -->
<div id="loadingSpinner">
    <div class="spinner-border" role="status">
        <span class="visually-hidden">Loading...</span>
    </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<!-- Spinner Logic -->
<script>
    const form = document.getElementById("recommendForm");
    const spinner = document.getElementById("loadingSpinner");

    form.addEventListener("submit", () => {
        spinner.style.display = "flex";
    });
</script>
</body>
</html>
